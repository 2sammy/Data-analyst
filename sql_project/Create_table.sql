-- Create the table
CREATE TABLE sales_data (
    order_id INT,
    order_date DATE,
    customer_id INT,
    product_name TEXT,
    category TEXT,
    region TEXT,
    quantity INT,
    unit_price FLOAT,
    sales FLOAT,
    profit FLOAT
);

CREATE TABLE sales_data (
    order_id INT, -- this is unique id for each order
    order_date DATE, -- Date the order was placed
    customer_id INT, -- this is unique for a customer
    product_name TEXT, --name of the product
    category TEXT,
    regio
)